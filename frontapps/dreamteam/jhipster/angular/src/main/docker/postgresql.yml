version: '2'
services:
    bpr-postgresql:
        image: postgres:9.6.5
        # volumes:
        #     - ~/volumes/jhipster/bpr/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=bpr
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
